server:
  port: ${PORT:8083}

spring:
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5433/finplanpro}
    username: ${DATABASE_USERNAME:postgres}
    password: ${DATABASE_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:bosstanasit2546@gmail.com}
    password: ${MAIL_PASSWORD:vlzy kuzf npfu bhgl}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID:649092656408-r4503okk8ghcecipn4embrhkd227hsso.apps.googleusercontent.com}
            client-secret: ${GOOGLE_CLIENT_SECRET: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}
            scope: email, profile

app:
  base-url: ${APP_BASE_URL:http://localhost:8083}

flyway:
  enabled: true
  baseline-on-migrate: true
  locations: classpath:db/migration
  clean-on-validation-error: true
